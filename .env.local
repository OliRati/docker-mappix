## Database configuration insite the Docker stack ( Do not touch )
DATABASE_URL="mysql://root:root@database:3306/mappix?serverVersion=8.0.32&charset=utf8mb4"

## Url for Vite server inside the Docker stack ( Do not touch )
VITE_DEV_SERVER_URL="https://0.0.0.0:5173"

## Url sent to Navigateur for Vite connexion
# Set it to the valid Url your Docker stack should respond)
VITE_DEV_ANNONCE_URL="https://localhost:5173"

# Select right mode dev or prod for Symfony server
# APP_ENV=dev
APP_ENV=prod
